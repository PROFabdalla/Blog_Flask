pip install flask
pip install - U Flask-SQLAlchemy
pip install PyMySQL
pip install flask-wtf         # form package
# pip install Flask-WTF
# flask --app app --debug run

pip install werkzeug  #hashed the password

pip install flask_login


# migrate the model
$ flask db stamp head
$ flask db migrate
$ flask db upgrade